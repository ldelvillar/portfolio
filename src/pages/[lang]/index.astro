---
import Layout from '@/layouts/Layout.astro';
import Hero from '@/components/home/Hero.astro';
import Experience from '@/components/home/Experience.astro';
import Projects from '@/components/home/Projects.astro';
import TechStack from '@/components/home/TechStack.astro';
import About from '@/components/home/About.astro';
import Certifications from '@/components/home/Certifications.astro';
import { getTranslations } from '@/utils/getTranslations';
import { DEFAULT_LANG, langStaticPaths, type Lang } from '@/i18n/config';

export async function getStaticPaths() {
  return langStaticPaths();
}

const lang = (Astro.params?.lang ?? DEFAULT_LANG) as Lang;
const t = getTranslations(lang);
---

<Layout title={t.site.title}>
  <main class="px-4">
    <Hero t={t} />
    <Experience t={t} />
    <Projects t={t} />
    <Certifications t={t} />
    <TechStack t={t} />
    <About t={t} />
  </main>
</Layout>
